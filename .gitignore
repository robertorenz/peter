# the full orchestral score (25 MB) — kept locally, not in git
IMSLP*.pdf
*.pdf

# audio drop-in (optional real recording)
peter-theme.mp3

# python/tooling artifacts
tools/themes_js.json
tools/themes_inline.js
__pycache__/

# OMR intermediates (heavy / regenerable) — keep omr/mid + themes/*.mid
omr.zip
omr/pages/
omr/xml/
omr/*.log
omr/progress.log
*.omr

# audio render toolchain (large downloads, regenerable)
tools/fluidsynth/
tools/fluidsynth.zip
tools/*.sf3
tools/*.sf2

# desktop (Electron) build — only the recipe is versioned
desktop/node_modules/
desktop/dist/
desktop/index.html
desktop/audio/
desktop/package-lock.json

# C64 port — toolchain download + build intermediates (the .prg/.d64 ARE versioned)
c64/bin/
c64/build/*.lst
c64/build/*.sym
c64/build/*.png
c64/build/*.inc
